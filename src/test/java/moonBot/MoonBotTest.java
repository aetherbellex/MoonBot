package moonBot;

/**
 * A class to test the Discord moon bot using JUnit.
 * 
 * @author Tasso
 *
 */
public class MoonBotTest
{
	/**
	 * TODO Test cases:
	 * Make sure you can connect (login) to Discord when using the correct bot token
	 * Make sure the connection fails and the program stops when an incorrect token is given
	 * Make sure that the bot can send messages to a channel
	 * Make sure that the bot can connect to the USNO API (maybe in a USNO test class?)
	 * 
	 */
}
